// Facade shim — the meeting-join page now lives in @rekindle/live so both the
// consumer and standalone ministry apps share one implementation.
export { default } from '@rekindle/live/components/MeetingJoinPage';
