// Barrel for @rekindle/types. Consumers generally import subpaths
// (e.g. `@rekindle/types/prayerTypes`) to avoid cross-module name collisions;
// this root export intentionally surfaces only the foundational language type.
export * from './language';
